import 'package:flutter/material.dart';

ThemeData themeData = ThemeData(
  scaffoldBackgroundColor: Colors.lightGreen.shade100,
  primaryColor: primary,
  cardTheme: CardTheme(color: Color(0xFF121212)),
);

final backgroundColor = Colors.lightGreen.shade100;
final primary = const Color.fromARGB(255, 157, 241, 181);

const textPrimary = Colors.black;
